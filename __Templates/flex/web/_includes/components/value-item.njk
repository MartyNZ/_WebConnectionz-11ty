{% for item in items %}
<div class="col-lg-4" data-aos="fade-up" data-aos-delay="200">
  <div class="box">
    <img src="{{item.valueImg}}" class="img-fluid" alt="">
    <h3>{{item.title}}</h3>
    <p>{{item.description|richtext|safe}}</p>
  </div>
</div>
{% endfor %}